<UserControl x:Class="steamcito.Views.LibraryView"
             x:Name="RootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:steamcito.ViewModels"
             
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"

             d:DataContext="{d:DesignInstance Type=local:LibraryViewModel}">

    <UserControl.Resources>
        <ResourceDictionary>
            
            <Style x:Key="ScrollThumbs" TargetType="{x:Type Thumb}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Height" Value="auto"/>
                <Setter Property="Width" Value="5"/>
                <Setter Property="Margin" Value="0 10 0 10"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Grid>
                                <Rectangle HorizontalAlignment="Stretch" 
                                           VerticalAlignment="Stretch"
                                           Width="Auto"
                                           Height="Auto"
                                           Fill="Transparent"/>
                                <Border x:Name="Rectangle1"
                                        Background="{TemplateBinding Background}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Width="Auto"
                                        Height="Auto"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Tag" Value="Horizontal">
                                    <Setter TargetName="Rectangle1" Property="Height" Value="7"/>
                                    <Setter TargetName="Rectangle1" Property="Width" Value="Auto"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>


                <Style x:Key="ScrollBar" TargetType="{x:Type ScrollBar}">
                    <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
                    <Setter Property="Foreground" Value="DimGray"/>
                    <Setter Property="Opacity" Value="0.2"></Setter>
                    <Setter Property="Width" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="GridRoot" Width="12" Background="{x:Null}">
                                    <Track Name="PART_Track" IsDirectionReversed="True" Focusable="False">
                                        
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton x:Name="PageUp" 
                                                          Command="ScrollBar.PageUpCommand"
                                                          Opacity="0"
                                                          Focusable="False"/>
                                        </Track.DecreaseRepeatButton>
                                        
                                        <Track.Thumb>
                                            <Thumb x:Name="Thumb" 
                                                   Style="{DynamicResource ScrollThumbs}"
                                                   Tag="Vertical"
                                                   Background="{TemplateBinding Foreground}"/>
                                        </Track.Thumb>
                                        
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton x:Name="PageDown" 
                                                          Command="ScrollBar.PageDownCommand"
                                                          Opacity="0"
                                                          Focusable="False"/>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                                
                                <ControlTemplate.Triggers>
                                    <Trigger SourceName="Thumb" Property="IsDragging" Value="True">
                                        <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource DarkBrush}"/>
                                    </Trigger>
                                    
                                    <Trigger SourceName="Thumb" Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource ButtonSelectBrush}"/>
                                    </Trigger>
                                    
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed"/>
                                    </Trigger>
                                    
                                    <Trigger Property="Orientation" Value="Horizontal">
                                        <Setter TargetName="GridRoot" Property="LayoutTransform">
                                            <Setter.Value>
                                                <RotateTransform Angle="-90"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter TargetName="PART_Track" Property="LayoutTransform">
                                            <Setter.Value>
                                                <RotateTransform Angle="-90"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Width" Value="Auto"/>
                                        <Setter Property="Height" Value="12"/>
                                        <Setter TargetName="Thumb" Property="Tag" Value="Horizontal"/>
                                        <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageRightCommand"/>
                                        <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/ContextMenuStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Background="#1B2838">

        <!-- COLUMNAS -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <!-- SIDEBAR -->
            <ColumnDefinition Width="*"/>
            <!-- MAIN -->
        </Grid.ColumnDefinitions>

        <!-- ===================== -->
        <!-- SIDEBAR -->
        <!-- ===================== -->
        <Grid Background="#171A21">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Search -->
                <RowDefinition Height="*"/>
                <!-- Lista -->
            </Grid.RowDefinitions>

            <!-- BUSCADOR -->
            <TextBox Margin="10"
                     Height="30"
                     Background="#2A475E"
                     Foreground="White"
                     BorderThickness="0"
                     Padding="5"
                     Text="Search..."/>

            <!-- LISTA DE JUEGOS -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                          Background="Transparent"
                          BorderThickness="1">
                <ListBox ItemsSource="{Binding Games}" 
                         SelectedItem="{Binding SelectedGame}"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="White"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Tag" Value="{Binding DataContext, ElementName=RootView}" />
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">

                                        <!-- PLAY -->
                                        <MenuItem FontWeight="SemiBold"
                                                  Foreground="White"
                                                  Command="{Binding PlayGameCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                            <MenuItem.Style>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                    <Setter Property="Header" Value="▶ Play" />
                                                    <Setter Property="Background" Value="#3d922e"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding PlacementTarget.DataContext.IsRunning, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True">
                                                            <Setter Property="Header" Value="◼ Stop" />
                                                            <Setter Property="Background" Value="#A03131"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsHighlighted" Value="True">
                                                            <Setter Property="Background" Value="#55c03d"/>
                                                        </Trigger>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding IsHighlighted, RelativeSource={RelativeSource Self}}" Value="True" />
                                                                <Condition Binding="{Binding PlacementTarget.DataContext.IsRunning, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True" />
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Background" Value="#C04040"/>
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </MenuItem.Style>
                                        </MenuItem>

                                       
                                        <MenuItem Command="{Binding ToggleFavoriteCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                            <MenuItem.Style>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                    <Setter Property="Header" Value="Add to favorites" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding PlacementTarget.DataContext.Details.Favorite, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True">
                                                            <Setter Property="Header" Value="Remove from favorites" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </MenuItem.Style>
                                        </MenuItem>
                                            
                                    
                                        
                                        <!-- Manage -->
                                        <MenuItem Header="Manage">

                                            <MenuItem Header="Add desktop shortcut"
                                                      Command="{Binding CreateShortcutCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />

                                            <MenuItem Header="Browse local files"
                                                      Command="{Binding OpenFolderCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext.GamePaths.FolderPath, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />

                                            <MenuItem Header="Remove"
                                                      Command="{Binding RemoveGameCommand}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />

                                       
                                        </MenuItem>
                                        <MenuItem Header="Properties..."
                                                  Command="{Binding OpenPropertiesCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                            
                                        </MenuItem>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="IconBorder" 
                                                Padding="10,8" 
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="2,0,0,0">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="IconBorder" Property="Background" Value="#3a424b" />
                                                <Setter TargetName="IconBorder" Property="BorderBrush" Value="#66C0F4" />
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="IconBorder" Property="Background" Value="#2b3138" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Details.Title}" 
                                       FontSize="14"
                                       Foreground="#8F98A0"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Grid>

        <!-- ===================== -->
        <!-- MAIN CONTENT -->
        <!-- ===================== -->
        <Grid Grid.Column="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="300"/> <!-- HEADER -->
                <RowDefinition Height="Auto"/> <!-- BOTONES -->
                <RowDefinition Height="*"/> <!-- INFO -->
            </Grid.RowDefinitions>

            <!-- HEADER (imagen grande del juego) -->
            <Grid Grid.Row="0">
                
                <Image Source="/Assets/header.jpg"
                       Stretch="UniformToFill"/>

                <!-- Overlay oscuro gradual -->
                <Rectangle>
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#00000000" Offset="0.5"/>
                            <GradientStop Color="#CC1B2838" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- Título -->
                <TextBlock Text="{Binding SelectedGame.Details.Title}"
                           Foreground="White"
                           FontSize="48"
                           FontWeight="Bold"
                           Margin="30,0,0,30"
                           VerticalAlignment="Bottom">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.8"/>
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>

            <!-- BOTONES -->
            <Border Grid.Row="1" Background="#1B2838" BorderBrush="#2A475E" BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal" Margin="30,15">

                    <Button x:Name="PlayButton" 
                            Width="160"
                            Height="45"
                            Foreground="White"
                            FontSize="18"
                            FontWeight="SemiBold"
                            BorderThickness="0"
                            Cursor="Hand"
                            Command="{Binding PlayGameCommand}"
                            CommandParameter="{Binding SelectedGame}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Content" Value="Play"/>
                                <Setter Property="Background" Value="#3d922e"/>
                                <Setter Property="OverridesDefaultStyle" Value="True"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedGame.IsRunning}" Value="True">
                                        <Setter Property="Content" Value="Stop"/>
                                        <Setter Property="Background" Value="#A03131"/>
                                    </DataTrigger>
                                    
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#55c03d"/>
                                    </Trigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                                            <Condition Binding="{Binding SelectedGame.IsRunning}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Background" Value="#C04040"/>
                                    </MultiDataTrigger>
                                    
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#66d04e"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="⚙"
                            Width="45"
                            Height="45"
                            Margin="15,0"
                            Background="#3d4450"
                            Foreground="White"
                            FontSize="20"
                            BorderThickness="0"
                            Cursor="Hand"
                            Command="{Binding OpenPropertiesCommand}"
                            CommandParameter="{Binding SelectedGame}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="3"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>

            <!-- INFO -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Visible">
                <ScrollViewer.Resources>
                    <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource ScrollBar}"/>
                    <Style TargetType="{x:Type Thumb}" BasedOn="{StaticResource ScrollThumbs}"/>
                </ScrollViewer.Resources>
                <Grid Margin="30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <!-- PANEL IZQUIERDO -->
                    <StackPanel>
                        <TextBlock Text="About the game"
                                   Foreground="#66C0F4"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Margin="0,0,0,15"/>

                        <Border Background="#171A21"
                                CornerRadius="5"
                                Padding="20"
                                MinHeight="200">
                            <TextBlock Text="{Binding SelectedGame.Details.Description, FallbackValue='No description available for this game.'}"
                                       Foreground="White"
                                       FontSize="14"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>

                    <!-- PANEL DERECHO -->
                    <StackPanel Grid.Column="1" Margin="30,0,0,0">
                        <TextBlock Text="Details"
                                   Foreground="#66C0F4"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="0,0,0,10"/>
                        
                        <StackPanel Margin="0,5">
                            <TextBlock Text="PLAYED TIME" Foreground="#8F98A0" FontSize="11" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectedGame.Details.PlayTime, StringFormat=\{0:hh\\:mm\} hs}" Foreground="White" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Margin="0,10">
                            <TextBlock Text="LAST SESSION" Foreground="#8F98A0" FontSize="11" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectedGame.Details.LastTime, StringFormat=\{0:dd/MM/yyyy\}, FallbackValue='Never'}" Foreground="White" FontSize="14"/>
                        </StackPanel>
                        <StackPanel Margin="0,10">
                            <TextBlock Text="STORE" Foreground="#8F98A0" FontSize="11" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectedGame.Details.Store,FallbackValue='Other'}" Foreground="White" FontSize="14"/>
                        </StackPanel>
                        <StackPanel Margin="0,10">
                            <TextBlock Text="HAS ORIGINAL DLL" Foreground="#8F98A0" FontSize="11" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectedGame.Details.IsSigned}" Foreground="White" FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>