<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================== -->
    <!-- COLORS & BRUSHES -->
    <!-- ===================== -->
    <SolidColorBrush x:Key="MenuBackground" Color="#2A2F38"/>
    <SolidColorBrush x:Key="MenuBorder" Color="#171A21"/>
    <SolidColorBrush x:Key="MenuItemHighlight" Color="#3A424D"/>
    <SolidColorBrush x:Key="MenuItemForeground" Color="#DCDCDC"/>
    <SolidColorBrush x:Key="MenuItemDisabledForeground" Color="#6D737A"/>
    <SolidColorBrush x:Key="MenuSeparator" Color="#3E4651"/>

    <!-- ===================== -->
    <!-- CONTEXT MENU -->
    <!-- ===================== -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource MenuBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MenuBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== -->
    <!-- SEPARATOR -->
    <!-- ===================== -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource MenuSeparator}"/>
        <Setter Property="Margin" Value="4,4"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}" 
                            Height="{TemplateBinding Height}" 
                            SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== -->
    <!-- MENU ITEM -->
    <!-- ===================== -->
    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource MenuItemForeground}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Height" Value="32"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid SnapsToDevicePixels="True">
                        <Border x:Name="Bg" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Margin="1,0"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="SubmenuArrow"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter ContentSource="Header"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>

                            <!-- Flecha para submenú -->
                            <Path x:Name="Arrow"
                                  Grid.Column="1"
                                  Data="M 0,0 L 4,4 L 0,8 Z"
                                  Fill="{TemplateBinding Foreground}"
                                  Margin="10,0"
                                  VerticalAlignment="Center"
                                  Visibility="Collapsed"/>
                        </Grid>

                        <!-- Popup del submenú -->
                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}"
                               Placement="Right"
                               VerticalOffset="-4"
                               HorizontalOffset="-2">
                            <Border Background="{StaticResource MenuBackground}"
                                    BorderBrush="{StaticResource MenuBorder}"
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    Padding="4">
                                <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                                    KeyboardNavigation.TabNavigation="Cycle"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="True">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource MenuItemDisabledForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource MenuItemHighlight}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>